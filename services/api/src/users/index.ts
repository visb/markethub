/**
 * API pública do contexto identity/users (story 47) — fachada de gestão de
 * usuários consumida pelo merchant (staff de loja). DI do módulo via
 * users.module direto.
 */
export * from "./admin-users.service";
