/**
 * API pública do módulo store-follows / contexto engagement (story 47) —
 * fachada de "seguir loja" consumida pela vitrine (catalog). DI do módulo via
 * store-follows.module direto.
 */
export * from "./store-follows.service";
