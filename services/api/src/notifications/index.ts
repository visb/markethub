/**
 * API pública do módulo notifications / contexto support (story 47) — fachada
 * de push consumida por outros contextos. DI do módulo via notifications.module
 * direto.
 */
export * from "./push.service";
