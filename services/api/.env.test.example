# Env de referência para a suíte e2e (services/api).
# O harness (test/setup-env.ts) já aplica defaults equivalentes em process.env;
# só defina TEST_DATABASE_URL se quiser apontar para outro banco (ex.: CI).
# NUNCA use o banco de dev — sempre um banco SEPARADO e descartável.

TEST_DATABASE_URL=postgresql://markethub:markethub@localhost:5433/markethub_test?schema=public
