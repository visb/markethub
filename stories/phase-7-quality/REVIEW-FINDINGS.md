# REVIEW-FINDINGS — achados arriscados (decisão humana)

Preenchido pelo run durante as unidades `B-*`. NÃO são auto-corrigidos — cada item muda
comportamento, contrato, schema ou não tem teste cobrindo. Revisar e aplicar manualmente.

Formato: `B-id · path:line · <sev> · problema · fix sugerido`
Severidade: `crit` (bug / regra de negócio) · `high` · `med` · `low`.

---

<!-- o run acrescenta itens abaixo, agrupados por severidade -->

## crit

- B01 · services/api/src/auth/dto/register.dto.ts:17 · crit · `/auth/register` é público e aceita `roles` com `admin`/`merchant` — escalação de privilégio por self-registration · restringir DTO a roles auto-registráveis (ex.: só `customer`) e criar staff/admin via rota protegida `@Roles("admin")`.
- B10 · services/api/src/payment/providers/pagarme.payment-provider.ts:108 · crit · `parseWebhook(payload)` ignora o argumento `signature` — não há verificação HMAC contra `PAGARME_WEBHOOK_SECRET` (env existe no CLAUDE.md mas não é usado). O endpoint `/webhooks/pix` é `@Public`, então qualquer um pode POSTar `{type:"charge.paid", data:{id:<chargeId>}}` e marcar um pedido como pago (→ markPaid → preparing → mercadoria liberada sem pagamento). Bypass de pagamento · receber `signature` e o corpo cru, calcular o HMAC com `PAGARME_WEBHOOK_SECRET` e rejeitar (retornar null/401) quando não bater; idem proteger o mock só em dev. Precisa do corpo raw (configurar rawBody no Nest) e de teste cobrindo assinatura inválida.

## high

- B20 · apps/admin/src/pages/*.tsx · high · O app admin inteiro NÃO segue o padrão obrigatório do CLAUDE.md: nenhuma das libs `@tanstack/react-query`, `react-hook-form`, `zod` está no package.json; todas as páginas fazem `api.request(...)` cru direto na tela com server-state em `useState`/`useEffect`/`useCallback` (ex.: `Orders.tsx:42`), sem `src/lib/queryKeys.ts`, sem módulos tipados em `src/api/`, sem hooks de query/mutation; formulários em `useState` (ex.: `ProductDetail.tsx`, 5x `useState`) em vez de RHF+zod; tipos de resposta redefinidos inline por página em vez de virem de `@markethub/types` · migração estrutural (fora do escopo de auto-fix do sweep, sem testes cobrindo): instalar React Query + RHF + zod, criar `queryKeys.ts` + módulos `src/api/`, extrair hooks e migrar páginas/forms incrementalmente conforme tocadas. Decisão humana / story dedicada.
- B21 · apps/customer/app/*.tsx · high · Mesmo desvio do B20, parcialmente menor: existe a camada de módulo tipado (`src/api/marketplace.ts` com factory `marketplace(api)`), então o HTTP não é cru na tela; mas faltam React Query, `react-hook-form` e `zod` (nenhuma no package.json, 0 `useQuery`/`useMutation`/`useForm`), não há `queryKeys.ts`, e as rotas orquestram server-state inline com `useState`/`useEffect`/`useCallback` (ex.: `app/home.tsx:32-45`) violando "telas não fazem fetch"; `use-cart.ts` mistura server-state com estado local · migração estrutural (não auto-fix): adicionar React Query + hooks de query/mutation sobre o módulo `marketplace` já tipado, `queryKeys.ts`, e RHF+zod nos formulários. Decisão humana / story dedicada.
- B22/B23 · apps/picker/app/*.tsx, apps/driver/app/*.tsx · high · Mesmo desvio sistêmico (idêntico nos dois apps): HTTP vai pelo client tipado compartilhado `@markethub/api-client` (`client.xxx()` via `useAuth`), o que é uma camada de dados válida; mas faltam React Query, `react-hook-form` e `zod` (nenhuma no package.json; 0 `useQuery`/`useMutation`/`useForm`), não há `queryKeys.ts` nem `src/api/`, e as telas orquestram server-state inline com `useState`/`useEffect`/`useCallback` (ex.: `picker/app/home.tsx:20-24`, `driver/app/home.tsx` 6× useState); formulários de login em `useState` · migração estrutural (não auto-fix): React Query + hooks + `queryKeys.ts` sobre o client tipado já existente, RHF+zod nos forms. Decisão humana / story dedicada.

## med

- B02 · services/api/src/users/admin-users.service.ts:122 · med · race no createStaff: `findUnique` email + `create` sem tratar `P2002` — criações concorrentes do mesmo email viram 500 em vez de 409 · capturar `P2002` no create e lançar `EMAIL_TAKEN` (mesmo padrão do finding B01 no register).
- B02 · services/api/src/users/admin-users.service.ts:26 · med · `page`/`pageSize` vêm como `Number(query)`; valor não-numérico → `NaN`, e `?? 1` não captura NaN (só undefined) → `skip: NaN` quebra a query Prisma com 500 · validar query com DTO (`@IsInt`/`@Min`) ou guardar `Number.isFinite` antes do clamp.
- B03 · services/api/src/catalog/catalog.service.ts:360 · med · mesmo bug de NaN do B02 no `paginate`: controllers passam `page ? Number(page) : undefined`, mas `"abc"`→`NaN` (truthy), `NaN ?? 1`→`NaN`, `Math.max(1, NaN)`→`NaN` → `skip: NaN` quebra Prisma com 500. Afeta `search`, `listStoreProducts`, `categoryFeed` e `admin-catalog.listProducts:28`. · guardar `Number.isFinite` antes do clamp (ou DTO de query com `@IsInt`/`@Min`), centralizado no `paginate`.
- B04 · services/api/src/erp/erp.service.ts:288 · med · `resolveCanonicalProduct` faz `findUnique({gtin})` + `create` em duas etapas; full-sync concorrente de lojas distintas com o mesmo GTIN viola a unique de `gtin` (`P2002`) e derruba o produto (`failed++`) em vez de reusar o canônico · usar `upsert` por `gtin` ou capturar `P2002` e re-buscar o existente.
- B05 · services/api/src/enrichment/enrichment.service.ts:224 · med · `resolveCategory` faz `findUnique(categoryMapping)` + `create` em duas etapas; jobs de enrich concorrentes para produtos com a mesma `cosmosCategory` violam a unique `source_sourceKey` (`P2002`) — no batch o `safeEnrich` engole, mas no `/enrichment/product?inline` vira 500 · usar `upsert` por `source_sourceKey` (ou capturar `P2002` e re-buscar).
- B06 · services/api/src/marketplace/orders.service.ts:194 · med · `markPaid` faz `order.update(preparing)` e `orderGroup.updateMany(preparing)` fora de transação, e o guard de idempotência (`status !== created && !== paid → return`) já passou; crash entre os dois writes deixa os grupos em `created`, e o re-disparo do webhook sai cedo (order já `preparing`) → grupos nunca avançam, ERP push e geração de picking nunca rodam · envolver os dois updates + push/picking numa `$transaction` idempotente, ou guardar por estado do grupo, não só do pedido.
- B06 · services/api/src/marketplace/orders.service.ts:143 · med · `list` tem o mesmo bug de NaN do B02/B03: `orders.controller` passa `page ? Number(page) : undefined`, `"abc"`→`NaN`→`Math.max(1,NaN)`→`NaN` → `skip: NaN` quebra Prisma com 500 · `Number.isFinite` antes do clamp (ou DTO de query). Recorrente no repo — vale um helper `parsePage` central.
- B07 · services/api/src/merchant/merchant-product.service.ts:61 · med · `create` faz `findUnique({gtin})` + `product.create` (+`attachOffer`) fora de transação; dois managers cadastrando o mesmo GTIN simultaneamente violam a unique de `gtin` (`P2002`) → 500. Além disso, se `attachOffer` falhar após `product.create`, fica Product órfão sem Offer/Stock · envolver create do produto + offer + stock numa `$transaction`; usar `upsert`/catch `P2002` no dedup por GTIN.
- B18 · services/api/src/admin/admin-dashboard.service.ts:30 · med · NaN em page/pageSize (mesmo bug recorrente B02/B03/B06) em `dashboard.orders` e `admin-merchants.storeOffers:331` — controllers passam `Number(page)`, `NaN` fura o `?? `/`Math.max` → `skip: NaN` → 500 · helper `parsePage` central com `Number.isFinite`.
- B08 · services/api/src/picking/picking-session.service.ts:175 · med · `recalcTotals` lê todos os `groups.subtotalCents` do pedido e reescreve `Order.itemsCents/totalCents` (read-modify-write fora de transação serializável); dois pickers concluindo grupos do mesmo pedido em paralelo causam lost update no total do Order · recalcular o total do pedido numa única transação (ou recomputar via agregação atômica), não a partir de um snapshot lido antes.
- B19 · services/api/src/common/codes.ts:8 · med · `shortCode` gera código de 4 dígitos (espaço de 10k) usado em `confirmPickup`/`confirmDelivered`/`handover`; essas rotas comparam o código sem limite de tentativas — brute-force viável (libera coleta/entrega indevida). O comentário reconhece o anti-brute-force como "Fase 4", mas a checagem já está em produção sem rate limit · adicionar limite de tentativas/bloqueio por OrderGroup (ou aumentar a entropia do código) antes de depender dele em prod.
- B02 · services/api/src/users/admin-users.controller.ts:49 · low · query `role` tipada como `RoleName` mas sem validação runtime (sem DTO/`@IsIn`) — role inválida passa direto pro filtro Prisma e retorna lista vazia silenciosa · validar `role` contra o enum `RoleName`. · med · race no register: `findUnique` + `create` sem tratar `P2002` — registros concorrentes do mesmo email viram 500 em vez de 409 · capturar `P2002` no create e lançar `EMAIL_TAKEN`.
- B01 · services/api/src/auth/auth.service.ts:145 · med · sessão criada com `refreshTokenHash: ""` e atualizada depois; crash entre os 2 writes deixa hash vazio e `argon2.verify("")` lança → refresh vira 500 em vez de 401 · criar sessão já com hash (gerar sid antes) ou envolver `verifyHash` em try/catch retornando false.
- B01 · services/api/src/auth/strategies/jwt.strategy.ts:18 · med · `validate` não revalida usuário — conta desativada mantém acesso até o access token expirar · aceitável com TTL curto; para revogação imediata, checar `user.active` no validate.

## low

- B01 · services/api/src/auth/auth.service.ts:54 · low · login sem usuário não roda `argon2.verify` — diferença de timing permite enumeração de email · verificar contra hash dummy quando user não existe.
- B01 · services/api/src/auth/auth.service.ts:114 · low · catch do logout engole também erro de DB do `updateMany` (falha de banco vira no-op silencioso) · restringir try/catch ao `verifyRefresh`.
- B03 · services/api/src/catalog/admin-catalog.controller.ts:35 · low · query `status` tipada `EnrichmentStatus` sem validação runtime (sem DTO/`@IsEnum`) — valor inválido cai direto no filtro Prisma de enum e estoura 500 em vez de 400 (mesmo padrão do finding B02 sobre `role`) · validar `status` contra o enum `EnrichmentStatus`.
- B03 · services/api/src/catalog/marketplace-category.service.ts:38 · low · `create` lança `NotFoundException` (404) com code `NAME_REQUIRED` para nome vazio — semântica errada (deveria ser 400). O DTO já tem `@MinLength(1)`, então a guarda é defensiva/morta na rota, mas o status fica errado se o service for reusado · trocar por `BadRequestException`.
- B03 · services/api/src/catalog/marketplace-category.service.ts:50 · low · `update`/`remove`/`assignRaw` fazem `assertExists` + mutate em duas etapas — deleção concorrente entre as duas causa `P2025` (500) em vez de 404 (race pattern-wide no repo) · capturar `P2025` na mutação e mapear para o `notFound` do módulo.
- B04 · services/api/src/erp/erp.service.ts:113 · low · `runPriceSync` faz `counters.updated++` mesmo quando `stripLocked` zera o patch (todos os campos travados) — métrica de "updated" do SyncRun fica inflada (conta itens não escritos) · só incrementar `updated` quando houve write efetivo.
- B09 · services/api/src/driver/driver.service.ts:35 · low · `listAssigned` (e `store-delivery.queue:28`) fazem `status as DeliveryStatus` a partir da query string sem validação runtime — valor inválido cai no filtro Prisma de enum e estoura 500 (mesma família de B02/B03) · validar `status` contra o enum `DeliveryStatus` (DTO `@IsEnum`).
- B11 · services/api/src/scheduling/scheduling.controller.ts:23 · low · `available` faz `new Date(date)` a partir da query string sem validar; `?date=garbage` → `Invalid Date` → filtro Prisma `start: { gte: <Invalid Date> }` estoura 500 · validar/parsear a data (retornar 400 em data inválida) antes de montar o filtro.
- B12 · services/api/src/reviews/reviews.service.ts:45 · low · `create` faz `findFirst(existing)` + `create` em duas etapas; dois POSTs concorrentes do mesmo eixo passam o check e ou criam avaliação duplicada (se não houver unique no schema) ou estouram `P2002` (500) em vez de `ALREADY_REVIEWED` · garantir unique `(orderId, axis, targetMerchantId)` no schema e capturar `P2002` mapeando para `ALREADY_REVIEWED`.
- B14 · services/api/src/notifications/notifications.controller.ts:28 · low · `unregister` (DELETE device-tokens) apaga por `token` do body sem checar dono (`removeToken` faz `deleteMany({ token })` sem `userId`) — usuário autenticado que conheça o token de outro pode removê-lo · escopar a remoção ao `userId` do `@CurrentUser` (`deleteMany({ token, userId })`).
- B18 · services/api/src/admin/admin-dashboard.controller.ts:7 · low · `toDate(s)=new Date(s)` em orders/finance/driver-tips sem validar (garbage → Invalid Date → filtro Prisma 500, mesma família de B11); `status as OrderStatus` sem validação runtime (enum 500); `createMerchant`/`updateMerchant` fazem findUnique(slug)+create/update — race P2002 no slug (admin, baixa concorrência) · validar data/enum nas queries; capturar P2002 no slug. Todos admin-only.
- B25 · packages/api-client/src/socket.ts:17 · low · `createRealtimeClient` é um stub que lança `"RealtimeClient not implemented yet — planned for Phase 5"`, mas o backend já emite eventos realtime (PickingGateway, OrderTrackingService.emit) e o CLAUDE.md referencia `@markethub/api-client/socket` como camada de realtime disponível — não há consumidor client funcionando, então o rastreio ao vivo (S5.1) não chega aos apps · implementar o cliente socket.io-client ou ajustar a doc/escopo se o realtime client ainda está fora do MVP.
- B24 · packages/types/src/auth.ts:5 · low · `registerSchema` (contrato compartilhado consumido pelos frontends) inclui `roles: z.array(roleNameSchema).optional()` aceitando qualquer role — espelha o problema do B01 (crit) no lado do contrato; um front poderia mandar `roles:["admin"]` e passar na validação client-side · ao corrigir B01, restringir também este schema a roles auto-registráveis (ex.: omitir `roles` ou limitar a `customer`).
- B19 · services/api/src/config/env.ts:56 · low · `STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY` têm default hardcoded (`markethub`/`markethub123`) — se a env de prod não setar, sobe com credencial fraca silenciosamente; além disso não há validação cruzada (ex.: `PAYMENT_PROVIDER=pagarme` mas `PAGARME_SECRET_KEY` opcional → undefined em runtime) · remover defaults de credencial (exigir em prod) e adicionar refinement cruzado no zod (secret obrigatório quando provider != mock).
- C27 · apps/driver/src/token-store.ts:7 · med (CORRIGIDO) · `SecureTokenStore` do driver era native-only (chamava `expo-secure-store` direto), e o expo-secure-store **lança no web** → o app driver não logava no Expo web (setTokens quebrava após o login 200, exibindo "Falha ao entrar"). picker/customer já tinham o fallback `Platform.OS==="web"`→localStorage. Achado pelo e2e-web C27 · CORRIGIDO espelhando o picker (branch web→localStorage em get/set/remove).
